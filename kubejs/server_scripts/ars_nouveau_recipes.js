ServerEvents.recipes(event => {

    // Triune Catalyst recipe MOVED to Extended Crafting (7x7 Elite Table).
    // See extended_crafting_capstones.js — the apparatus route was removed so
    // the EC recipe is the catalyst's only source (extreme-crafting capstone).

})